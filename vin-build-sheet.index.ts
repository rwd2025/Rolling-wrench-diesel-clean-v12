import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
function year(v:string){const m:any={A:2010,B:2011,C:2012,D:2013,E:2014,F:2015,G:2016,H:2017,J:2018,K:2019,L:2020,M:2021,N:2022,P:2023,R:2024,S:2025,T:2026};return m[v?.[9]]||null}
function make(v:string){v=(v||"").toUpperCase();if(v.startsWith("1NK"))return"Kenworth";if(v.startsWith("1XP"))return"Peterbilt";if(v.startsWith("3AK")||v.startsWith("1FU"))return"Freightliner";if(v.startsWith("2HS")||v.startsWith("3HS")||v.startsWith("1HT"))return"International";return"Truck"}
serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const {vin}=await req.json();const clean=String(vin||"").toUpperCase().match(/[A-HJ-NPR-Z0-9]{17}/)?.[0];if(!clean)throw new Error("VIN required");return new Response(JSON.stringify({vin:clean,year:year(clean),make:make(clean),type:"Truck",source:"local-fallback",confidence:"Medium",notes:["Set VIN_API_URL secret for full OEM build sheet."]}),{headers:{...cors,"Content-Type":"application/json"}})}catch(e){return new Response(JSON.stringify({error:e.message}),{status:400,headers:{...cors,"Content-Type":"application/json"}})}});